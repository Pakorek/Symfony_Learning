<ul>
    {% for category in categories %}
        <li>
            <a href="{{ path('wild_show_category', {'categoryName': category.name}) }}">
                {{ category.name }}
            </a>
        </li>
    {% endfor %}
</ul>